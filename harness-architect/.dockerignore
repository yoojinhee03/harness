# 빌드 컨텍스트 슬림화 — 백엔드 이미지에 불필요한 것들 제외.
.venv/
.git/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 로컬 시크릿은 이미지에 넣지 않는다(키는 런타임 env 로 주입).
.env

# 프론트 소스는 별도 이미지(apps/web/Dockerfile)에서 빌드하므로 백엔드 컨텍스트에서 제외.
apps/web/

# 문서/도커 메타는 런타임에 불필요.
docs/
Dockerfile
.dockerignore
